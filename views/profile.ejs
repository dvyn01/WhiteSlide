<% include partials/header %>

<div class="container">
    <h1 style="text-align: center;">Welcome, <%= currentUser.firstName %>!</h1>
    <div>
        <h2>
            <strong>
                Your Rooms:
            </strong>
        </h2>
        <br>
        <% currentUser.rooms.forEach(room => { %>
        <%var Id = room._id%>
        <% rooms.forEach(room => { %>
        <%if(room._id.toString() == Id) {%>
        <li>
            <a href="/<%= room.roomId%>">
                <%= room.roomId%>
            </a>
        </li>
        <% }%>
        <%});%>
        <br>
        <% }); %>
    </div>
</div>

<% include partials/footer %>